apiVersion: v1
kind: Service
metadata:
  name: moon-bot
  namespace: moon-bot
  labels:
    app.kubernetes.io/name: moon-bot
    app.kubernetes.io/component: service
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: moon-bot
    app.kubernetes.io/component: app
  ports:
    - name: http
      port: 3001
      targetPort: http
