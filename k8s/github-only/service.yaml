apiVersion: v1
kind: Service
metadata:
  name: ishu-github-only
  namespace: ishu
  labels:
    app.kubernetes.io/name: ishu
    app.kubernetes.io/component: github-bot-service
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: ishu
    app.kubernetes.io/component: github-bot
  ports:
    - name: webhook
      port: 3000
      targetPort: webhook
